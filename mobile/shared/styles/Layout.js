import { StyleSheet } from 'react-native';

export const Layout = StyleSheet.create({
  centeringContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  roundedCorners: { borderRadius: 10 },
});
